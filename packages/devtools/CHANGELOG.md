# Changelog

## 0.1.0 (unreleased)

- Extract the optional inspector into `@slicemedia/devtools` with an independent package version.
- Ship inert ESM exports, TypeScript declarations, packaged documentation, and a standalone
  `devtools.global.js` browser script with embedded styles and dependencies.
- Preserve the launcher, draggable/resizable panel, opacity and activation preferences, structured
  requirements, runtime diagnostics, duplicate warnings, and manual rescanning.
